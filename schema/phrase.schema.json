{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/jmilbery/boston-translatah/schema/phrase.schema.json",
  "title": "Phrase entry",
  "description": "A canonical multi-word rendering that does not derive from the pronunciation rules.",
  "type": "object",
  "required": ["phrase", "local", "region", "sources"],
  "additionalProperties": false,
  "properties": {
    "phrase": { "type": "string", "minLength": 1, "description": "Plain English." },
    "local": { "type": "string", "minLength": 1, "description": "The local rendering, in whatever region this entry belongs to." },
    "respelling": { "type": "string", "description": "Optional pronounce-it-out-loud respelling." },
    "region": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "description": "Region slug. Must be registered in data/regions.yml."
    },
    "notes": { "type": "string" },
    "sources": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["type", "ref"],
        "additionalProperties": false,
        "properties": {
          "type": { "type": "string", "enum": ["url", "vouch", "book"] },
          "ref": { "type": "string", "minLength": 1 }
        }
      }
    }
  }
}
